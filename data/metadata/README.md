# Data metadata

For each raw data set, record its source, license, retrieval date, variable
definitions, frequency, transformations and checksum. These text metadata are
version controlled even when the licensed raw observations are not.
